import javax.sound.sampled.SourceDataLine;

public class str {
    public static void main(String[] args) {
        int i,sum=0,n=str.Length();
        for(i=0;i<n;i++)
        {
            char ch=str.charAt();
            if(ch)='o' && ch<='a';
            sum=sum+(ch-'o')
        }
        System.out.println("sum="+sum);
    }

}
